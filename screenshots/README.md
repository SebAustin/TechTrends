# Screenshots

| File | Description |
|------|-------------|
| `docker-run-local.png` | Real capture of the app at `http://127.0.0.1:7111` (Docker run). |
| `ci-github-actions.png` | Replace with your successful **TechTrends - Package with Docker** workflow run. |
| `ci-dockerhub.png` | Replace with Docker Hub showing `techtrends:latest`. |
| `k8s-nodes.png` | Replace with `kubectl get nodes` from your k3s Vagrant VM. |
| `kubernetes-declarative-manifests.png` | Replace with `kubectl get all -n sandbox`. |
| `argocd-ui.png` | Replace with Argo CD UI after login (nav bar visible). |
| `argocd-techtrends-staging.png` | Replace with synced **techtrends-staging** application. |
| `argocd-techtrends-prod.png` | Replace with synced **techtrends-prod** application. |

The CI/K8s/Argo CD PNGs were generated as labeled placeholders so filenames exist before you run those steps locally.
